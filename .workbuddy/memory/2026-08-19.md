# 2026-08-19

## 多语言（i18n）文案完整性审查与补齐
- 项目 meerkat-crm 多语言分两套：前端用 i18next（frontend/src/i18n/locales/*.json，5 语言 en/de/it/es/fr），后端用自研包（backend/i18n/locales/*.json，同 5 语言）；语言经由 users.language 字段在设置页/登录时联动。
- 用脚本递归 diff key 后发现：前后端各 5 语言 key 集合、顺序、空值原本就完全对齐（前端 765、后端 31），无缺失/多余。
- 反向扫描代码引用发现 4 个真正的遗漏（t() 带 fallback 默认值，缺失导致非英语用户看到英文）：
  - 前端 `contacts.add.email` / `contacts.add.firstname` / `contacts.add.lastname`（ImportContactsDialog.tsx、MonicaImportDialog.tsx 引用，值同 contacts.*）
  - 前端 `contacts.import.upload.supportedFormats`（ImportContactsDialog.tsx 引用，新功能文案）
- 已补齐：在 5 个前端 locale 文件的 `contacts.add` 下加 email/firstname/lastname，在 `contacts.import.upload` 下加 supportedFormats（各语言译文），前端 key 总数变 769，校验后 5 语言对齐、代码引用缺失=0。
- 注意：`contacts.add.*` 与 `contacts.*` 现存在语义重复（值相同）。若要消除冗余，可后续将代码引用改为 contacts.firstname/lastname/email 并删除 contacts.add.* 三个 key（属重构，本次未做）。
- 另有"值与 en 相同"的项（de 21 / it 18 / es 17 / fr 36）多数为合理保留（品牌名 Meerkat CRM、Email/Fax/ID/URL/Zoom/Dashboard 等技术术语与通用外来词），非 bug，未改动。
- 验证脚本：`/tmp/check_i18n.py`（前后端 key 集合对齐）、`/tmp/check_i18n2.py`（空值/同 en/顺序）、`/tmp/check_i18n3.py`（代码引用 vs en）。

## 合并 contacts.add.* 到 contacts.*（消除冗余）
- 用户要求把 `contacts.add.email/firstname/lastname` 合并进已存在的顶层 `contacts.email/firstname/lastname`，其余不动。
- 改动：从 5 个前端 locale 文件的 `contacts.add` 块中删除 email/firstname/lastname 三项（保留原 createBirthdayReminder 及 button/title/create 等原有关键字，并修正其尾随逗号）；将 ImportContactsDialog.tsx、MonicaImportDialog.tsx 中 6 处 `t('contacts.add.firstname/lastname/email')` 改为 `t('contacts.firstname/lastname/email')`。
- `contacts.import.upload.supportedFormats` 按"其余不动"保留。
- 验证：代码已无 `contacts.add.email/firstname/lastname` 引用；5 前端语言 key 总数由 769 回落至 766（删除 3），0 缺失/0 多余；代码引用缺失=0。

## 新增中文/日语/韩语三种语言（前后端）
- 用户要求新增 zh/ja/ko 三语言，强调"再三比对、用词贴合个人用户（这是管亲友的个人 CRM）、专业性次要"，前后端都要改。
- 生成策略：用 /tmp/gen_locales.py 字典（键=精确英文叶子串，值=[zh,ja,ko] 三元组）重建 JSON，保证结构/key/顺序严格对齐且 JSON 合法；首跑报 15 前端+21 后端"缺失"均为字典键名写错/后端误写成点路径键，补录真实值后重跑=0 缺失。
- 产出文件：
  - 前端 frontend/src/i18n/locales/{zh,ja,ko}.json（各 692 叶子 key）
  - 后端 backend/i18n/locales/{zh,ja,ko}.json（各 24 叶子 key）
- 注册/接线改动（前后端各一处）：
  - 前端 config.ts：补 3 条 import（zh/ja/ko.json）+ 在 resources 内加 zh/ja/ko 三块（fallbackLng='en' 不变）
  - 前端 SettingsPage.tsx：语言下拉补 `<MenuItem value="zh">中文</MenuItem>` / `ja` 日本語 / `ko` 한국어
  - 后端 i18n/i18n.go：`SupportedLanguages` 加 "zh","ja","ko"（DefaultLanguage 仍 "en"；normalizeLanguage/IsValidLanguage 自动覆盖）
  - 后端校验 oneof：models/user.go:14、models/dtos.go:157、controllers/user_controller.go:369 的 language 字段 oneof 加 zh ja ko；user_controller.go:389 错误文案动态引用 SupportedLanguages，自动覆盖。
- 验证（/tmp/validate_i18n.py）：8 前端（692）+8 后端（24）locale 与 en key 集合完全一致（0 缺失/0 多余），全仓无 `__MISSING__` 残留；代码引用扫描的"缺失"均为正则误报（.get('search') 等子串命中 t('search')、monicaImport.gift.+status 动态拼接，实际 key 存在）。
- 注：DB 无 language 列 CHECK 约束，仅靠应用层校验 + 列默认值 'en'；新增语言无需改表结构。

## 重点审校 zh/ja/ko 译文 + 微调（用户要求"重点看 zh，再抽查其他"）
- 自动核查结论（脚本 /tmp/spotcheck_i18n.py、/tmp/cjk_check.py）：
  - 占位符 {{...}} 前后端 ja/ko 与 en 100% 一致，无变量丢失。
  - "ja 误含中文字符""ja==zh"两类告警均为误报：日语本就用汉字（連絡先/設定/保存/名/姓…），非抄错。
  - ko 含中文字符 0 条（纯谚文，确为真翻译）；ko==zh 0 条。
  - ja/ko 与 en 完全相同仅 4 条且全为合理保留（Meerkat CRM / Monica URL / URL / ID）；后端 0 条。
- 用户拍板按建议微调，已改 3 处：
  - ja.json `relationships.types.brother` 兄→**兄弟**、`sister` 姉→**姉妹**（避免仅指哥哥/姐姐的语义收窄；中文 brother/sister 原本已是泛化"兄弟/姐妹"，无需改）。
  - zh.json `relationships.types.spouse` 配偶→**伴侣**（个人 CRM 更亲切包容）。
  - zh.json `settings.contactFields.groups.meerkat` 笔记→**备注**（上游 key 名 meerkat 是怪命名，值原映射"笔记"费解，仅中文改清晰；ja/ko 各自为 メモ/메모 本就合适，不动）。
- 复查：validate_i18n.py 仍 PASS（key 集合与 en 完全对齐、JSON 合法）。
